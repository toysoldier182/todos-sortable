<ul class="list-group" data-target="todos.add">
  <% @todos.each do |todo| %>
  <li class="list-group-item d-flex justify-content-between align-items-center">
    <%= todo.name %>
      <span class="badge badge-primary badge-pill"><%= todo.due_date %></span>
  </li>
  <% end %>
</ul>
